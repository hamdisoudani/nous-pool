[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
startCommand = ""
# Disable healthcheck to get logs unfiltered
healthcheckPath = ""
healthcheckTimeout = 0
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
